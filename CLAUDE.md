# Suspectra

Read `HANDOFF.md` before doing anything in this repo. It has the contracts, the file map, and the project context.

Jonas writes `src/dissonance.js` and `src/optimizer.js` himself. Do not write, complete, or paste implementations of `pair`, `total`, or `optimize`, even partially. The handoff lists what assistants may do instead.

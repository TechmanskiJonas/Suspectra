# Suspectra

A 12-TET browser instrument whose overtones move to fit the chord you're holding.

Open `index.html` in Chrome. Click keys to toggle notes, or connect a MIDI keyboard.

- `src/dissonance.js` and `src/optimizer.js` are the research code (in progress).
- Everything else is the instrument shell.
